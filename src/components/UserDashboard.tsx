import HeroSection from './HeroSection';

const UserDashboard = () => {
      return (
            <div>
                  <HeroSection />

            </div>
      );
};

export default UserDashboard;